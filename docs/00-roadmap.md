# 00 路线图(项目蓝图)

> 先读这张地图,再读 `01–08` 规格。本文是里程碑级的全局站位与终点线,**不复述切片**(切片在 GitHub Issues),也**不是第二份 spec**(口径仍以 `01–08` 为准)。状态会变的细节(切片、HITL 待办值)留在 issue 与执行记忆,本文只给"走到哪、往哪去"。

## 分期词汇(唯一口径)

本项目统一用 **里程碑 M1–M5 + GA 终点线** 描述进度。历史文档里的「一期 / 二期」已退役,对照如下:

| 历史词 | 现含义 |
| --- | --- |
| 一期 | v2 SDK 本体(M1–M4),即"最小化 AAR 的可商用功能集" |
| 二期 | 拆分为三类:**M4 已交付**(生产 AAR 纯净化/运行模式隔离)、**GA 受控验收**(真实平台/账户/支付/充值回调线上联调)、**v2 范围外·未来**(真实用户中心 H5、设备标识、广告归因、第三方监控) |

## 三轨总览

```
主轴 · v2 SDK(Android AAR + 平台服务端 SDK 网关面)
  M1 ──► M2 ──► M3 ──► M4 ──►  ◇ GA 受控验收(进行中)
  ✓      ✓      ✓      ✓        客户端就位;余服务端生产支付→回调接线(#58)+ 真实资金/密钥/实名厂商等受控确认

支轨 A · 用户中心平台面(uc SPA + 平台对玩家 API 面 /api/uc/v2)
  ✓ dev 贯通 ──► /api/uc/v2 六端点上 dev(CTID 105)+ uc SPA USE_MOCK=false 上线
                (CTID 107),网关铸 token→uc 真读写全链 live 验证;口径 06a / ADR-0010

支轨 B · 公会运营平台(原 M5,独立产品面)
  规划中(#32)──► 复用 M4 归因地基,独立排期,【不阻塞】主轴 GA
```

三条终点线相互独立:**(A)v2 SDK GA** = 生产受控验收 6 项闭合;**(B)用户中心平台面 v1** = uc SPA 真数据贯通(`/api/uc/v2` 全面 + SPA `USE_MOCK=false`)— **dev 已达成(2026-06-13)**,生产 uc 面随主轴 GA 真平台一并落地;**(C)公会平台 v1** = 独立里程碑。互不阻塞 SDK 商用。

> **支轨 A 与 v2 范围**:用户中心平台面是「平台对玩家」侧产物(`/api/uc/v2` 与 SDK 网关面 `/api/sdk/v2` 并列且独立,ADR-0010),**不进 AAR**。SDK 侧只保留「H5 容器 + bridge」(M3 已交付);uc SPA 由 SDK WebView 加载。若将来要把该面纳入 **v2 正式出货**(而非作为独立平台面),须按 `01 §5` 重走能力白名单/公开 API/Manifest/依赖/接入自检评审。

## 主轴里程碑(v2 SDK)

| 里程碑 | 产出 | 状态 | 入口 |
| --- | --- | --- | --- |
| **M1 双端贯通最小核** | `config`/`sms-codes`/`account-sessions` + HMAC 入站签名 + dev 控制面 + 冷启动切片(init→维护门禁→协议告知→登录窗→真实 devCode 登录);服务端部署 dev CTID 105 | ✓ 完成 | #1 |
| **M2 登录后链路到游戏进入** | 账户检查 / 实名 / 防沉迷进入门禁 / 游戏小号体系 / 小号登录;公开 API 定型 | ✓ 完成 | #10 |
| **M3 运营闭环** | 支付 + 角色上报全链 + 充值回调分发(客户端回调≠发货依据)+ 悬浮球/用户中心 H5 容器 + 密码登录 + 防沉迷支付门禁;公开 API 收口 | ✓ 完成 | #20 |
| **M4 发布门禁与生产化** | 单模块合并(ADR-0006)· dev/prod 交付变体 · `verifyPublicAarPurity` 五维门禁+元测试 · 服务端生产加固(密钥注入/build-tag/实名 fail-closed)· 渠道三件套(两源读取/`channel-pack` 免重签/归因 e2e)· CTID 106 生产部署 · 一条命令发布+接入文档+交接清单(注:生产支付→回调**接线**不含在 M4 生产加固内,属 GA 前置 #58) | ✓ 完成(merge `872f26d`) | #33 |
| **◇ GA 受控验收** | v2 SDK 商用终点线:6 项真实链路受控确认(见下),确认本身属 HITL/运维/业务(非开发);服务端开发前置 #60 已闭合(PR #65 合并 main),**商户资质已通过(2026-06-14)**;微信预下单出网接线**代码已接(PR #67)**、支付宝就绪;剩 AAR 外跳 #61 + 密钥注入/部署/小额真单 | 进行中 | GA 跟踪 issue |

### GA 终点线:生产受控验收 6 项

口径见 `docs/production-controlled-acceptance-handoff.md`(02「生产受控验收」定义)。客户端 AAR 已"结构正确、能力纯净、fail-closed";服务端密钥注入 fail-closed、实名厂商接线、充值回调**发送端 + 漏发自愈巡检**、业务日志(#57 已闭合)、**生产入站支付链路(#60 已闭合并合并 main,PR #65:自建收银台 + 微信/支付宝预下单 + webhook 接收端)**均已就位;**商户资质亦已通过(2026-06-14)**。剩余前置见下:

1. 生产密钥检索与注入(运维)— 资质已通过、真实密钥可注入;fail-closed,注入即可、无需改码
2. 真实资金通道联调(平台+资金运营)— ✅ #60 服务端已闭合、资质已通过;✅ **微信预下单出网调用已接线**(PR #67:503 桩→真实 `/v3/pay/transactions/h5`),支付宝 wap 早已就绪;⚠️ 剩 **AAR 支付域外跳 #61**(拉起渠道 App)+ 真实密钥注入触发真单
3. 平台→游戏服务端充值回调真实链路(平台+游戏服务端)— ✅ #60 已闭合(渠道 webhook → `CompletePayment` → 充值回调 + 漏发自愈巡检);剩真单触发
4. 发货幂等验证(游戏服务端)— 游戏服务端职责;平台侧符合规格
5. 平台日志检索(运维/平台)— 服务端业务日志已就位(#57 已闭合:下单/回调链路结构化 slog,可按订单号/脱敏 account 检索),余运维侧日志通道检索
6. 实名核验真实厂商对接(平台采购)— NPPA 客户端已接线,填 per-game 凭据即可、无需改码

服务端开发前置 **#60**(自建收银台 + 微信/支付宝预下单 + webhook 接收端 + 漏发自愈)**已闭合并合并 main(PR #65)**;**商户资质已通过(2026-06-14)**。微信预下单出网接线**代码已接(PR #67)**、支付宝就绪;剩 **#61** AAR 支付域外跳例外/`01 §5` 评审 + 密钥注入/部署/小额真单受控确认,即达 **v2 SDK GA**。outbound 回调签名定稿见 **#59**(游戏服务端对接,并行)。

> 经 grill(2026-06-14)细化:支付集成定为「平台自建收银台 + 微信/支付宝直连」,故 #61 须为支付 WebView 开「支付域受限外跳」(拉起微信/支付宝 App)——这会再动一次「最小化 AAR」身份,走 `01 §5` 评审 + ADR。

## 支轨 A:用户中心平台面(uc SPA + /api/uc/v2)

| 项 | 内容 |
| --- | --- |
| 定位 | 「平台对玩家」侧产物:用户中心远程 H5 单页(uc SPA)+ 平台对玩家数据 API 面(`/api/uc/v2`)。**不进 AAR**;SDK 侧只保留 H5 容器 + bridge(M3 已交付),uc SPA 由 SDK WebView 加载。 |
| 范围 | 以主账户为核心:身份/账号安全/换绑手机/修改密码/充值订单等(06a)。 |
| 工程落位 | `uc/` 纯静态 SPA(无构建)部署 CTID 107(`uc.xingninghuyu.com`);`/api/uc/v2` 在现服务端 `internal/api`(`platformToken` Bearer 鉴权、不走 HMAC,与网关面并列独立,ADR-0010)。 |
| 口径 | 06a(SPA 信息架构/数据 API)、ADR-0010(API 面)、06(容器与 bridge)。 |
| 状态 | **v1 dev 贯通达成(2026-06-13)**:`/api/uc/v2` 六端点(profile / orders / 换绑手机 / 改密)部署 dev(CTID 105);uc SPA `USE_MOCK=false` 走绝对域真接口、上线 CTID 107;网关铸 token → uc 真读(profile/orders)+ 真写(换绑回显新号、不登出)全链手动 live 走通。**留存证据已入库**(`docs/assets/acceptance-uc/`:主页真机实拍 + dev 贯通证据记录);uc SPA 四页正式截图验收属支轨 A 自身、另行收口(#53)。生产 uc 面随主轴 GA 真平台一并落地(#56)。 |
| 关系 | **不阻塞** v2 SDK GA;独立排期。纳入 v2 正式出货须走 `01 §5` 评审。 |
| 入口 | 已并入 main(merge `6e6a6d1`):uc SPA + /api/uc/v2 + 部署 + WebView 加载态(#50/#51)。 |

## 支轨 B:公会运营平台(原 M5)

| 项 | 内容 |
| --- | --- |
| 定位 | 推广公会会长管理平台(独立产品面,非 SDK) |
| 范围 | 落地页动态渠道包归因 + 数据看板 + 分成对账 |
| 工程落位 | `console-server` 独立二进制 + 独立子域名(立项时修 ADR-0003、扩 docs) |
| 地基 | 复用 M4 渠道三件套(SDK 两源读取 + `channel-pack` 写入 + 归因 e2e) |
| 关系 | **不阻塞** v2 SDK GA;独立排期 |
| 入口 | #32(规划,8 条冻结决议;尚无 PRD) |

## v2 范围外 · 未来

以下不在 v2 产物内,进入前须重走能力白名单/公开 API/Manifest/依赖/接入自检评审(`01 §5`):设备标识采集或透传(OAID/Android ID)、广告归因、第三方监控;**平台余额/余额支付(平台币体系,玩家预存真实货币、下单从余额扣减)**——经 grill(2026-06-14)定为范围外·可拓展项,v2 不自持余额、走持牌机构直连代收,合规定性(储值=备付金/二清/支付牌照)与前向兼容缝见 ADR-0012;以及 `01 §4.2` 的永久排除项(客服/IM、平台 App 跳转、活动优惠券、强更、加速连点器、通用 H5 文件/媒体/外跳)。(用户中心远程 H5 平台面已立为**支轨 A**,见上;它是平台对玩家侧产物、不进 AAR,故不在「v2 AAR 产物」内但已正式在册。)
